spring:
  redis:
    database: 0 # Redis数据库索引（默认为0）
    timeout: 60s  # 连接超时时间
    host: 192168.33.21  # Redis服务器地址
    port: 6379  # Redis服务器连接端口
    password:   # Redis服务器连接密码（默认为空）
    jedis:
      pool:
        max-idle: 500
        min-idle: 50
        max-wait: -1s
        max-active: -1
